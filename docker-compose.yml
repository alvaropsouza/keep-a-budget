version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: keep-a-budget-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: keep-a-budget
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/sql:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d keep-a-budget"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - keep-a-budget-network

  minio:
    image: minio/minio:latest
    container_name: keep-a-budget-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/minio_data
    command: server /minio_data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - keep-a-budget-network

volumes:
  postgres_data:
    driver: local
  minio_data:
    driver: local

networks:
  keep-a-budget-network:
    driver: bridge
